# Processed data

Put generated training, validation, and test files here.

Do not treat this directory as the source of truth. If a file can be rebuilt from `raw/` and `scripts/`, keep the script and ignore the generated data.
