# Matrix factorization report

This report has not been generated yet.

Run the experiment from the repository root:

```bash
./01-traditional-statistics/matrix-factorization/run.sh --sample-ratings none
```

The script will overwrite this file with real metrics and embedding-neighbor examples.

The generated report will include `.pt` file sizes when checkpoints are saved. This is useful beginner experience: embedding models can be small or surprisingly large depending on user count, movie count, and embedding dimension.
