# Two tower retrieval report

This report has not been generated yet.

Run the experiment from the repository root:

```bash
./02-retrieval/two-tower-tfrs/run.sh --sample-ratings none --save-checkpoints --checkpoint-every 0
```

The script will overwrite this file with real metrics, retrieved candidates, and checkpoint file sizes.

