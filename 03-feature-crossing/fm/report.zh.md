# FM 因子分解机实验报告

这个报告还没有生成。

从仓库根目录运行：

```bash
./03-feature-crossing/fm/run.sh --sample-ratings none --num-workers 8 --save-checkpoints --checkpoint-every 0
```

运行结束后，这个文件会被真实的验证指标、测试集预测样例和 `.pt` checkpoint 大小信息覆盖。

